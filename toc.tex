$if(toc)$
$if(toc-title)$
\renewcommand*\contentsname{$toc-title$}
$endif$

$if(beamer)$
\begin{frame}[allowframebreaks]
$if(toc-title)$
  \frametitle{$toc-title$}
$endif$
  \setcounter{tocdepth}{$toc-depth$}
  \tableofcontents
\end{frame}
$else$
{
$if(colorlinks)$
\hypersetup{linkcolor=$if(toccolor)$$toccolor$$else$$endif$}
$endif$
\setcounter{tocdepth}{$toc-depth$}

% Load tocloft package for TOC customization

\makeatletter

% Define a new font for normal Calibri
\newfontfamily\calibriRegular{Calibri} 

% Define TOC title format 
\renewcommand{\cfttoctitlefont}{\calibriLight\color{PRBBlue}\fontsize{16pt}{12pt}\selectfont\bfseries\MakeUppercase}
\setlength{\cftbeforetoctitleskip}{0pt}  % Ensure no extra space before title
\setlength{\cftaftertoctitleskip}{12pt}  % Add 12pt space after title

% Define Chapter level (Normal Calibri, bold, PRBBlue)
\renewcommand{\cftchapfont}{\calibriRegular\bfseries\fontsize{12pt}{14pt}\selectfont\color{PRBBlue}}
\renewcommand{\cftchappagefont}{\color{PRBBlue}}
\setlength{\cftbeforechapskip}{0pt}  % Space before chapter entries

% Define Level 1 sections in TOC (fontsize 12pt, PRBBlue, bold)
\renewcommand{\cftsecfont}{\calibriLight\itshape\fontsize{11pt}{13pt}\selectfont\color{PRBBlue}}
\renewcommand{\cftsecpagefont}{\color{PRBBlue}}
\setlength{\cftbeforesecskip}{0pt}   % Adjust space after section entries if needed

\makeatother

\tableofcontents
}
$endif$
$endif$
$if(lof)$
\listoffigures
$endif$
$if(lot)$
\listoftables
$endif$


